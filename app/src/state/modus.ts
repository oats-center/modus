// things that are too big to fit in the state without crashing browser
export const paths = {
  Grower: '$.source.grower.name',
  Farm: '$.source.farm.name',
  Field: '$.source.field.name',
  'Source Filename': '$.lab.files[*]',
}
